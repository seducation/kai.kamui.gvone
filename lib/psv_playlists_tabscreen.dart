import 'package:flutter/material.dart';

class PsvPlaylistsTabscreen extends StatelessWidget {
  const PsvPlaylistsTabscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Playlists Content"),
    );
  }
}
